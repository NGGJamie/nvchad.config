local options = {
  ensure_installed = { "lua", "rust", "javascript", "typescript", "c", "python" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
